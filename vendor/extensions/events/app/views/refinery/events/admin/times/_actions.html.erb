<ul>
  <% if ::Refinery::Events::Admin::TimesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.events_admin_times_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_events_admin_time_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Events::Admin::TimesController.sortable? && ::Refinery::Events::Time.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Times"),
                 refinery.events_admin_times_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Times"),
                 refinery.events_admin_times_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
