<ul>
  <% if ::Refinery::Events::Admin::TypesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.events_admin_types_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_events_admin_type_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Events::Admin::TypesController.sortable? && ::Refinery::Events::Type.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Types"),
                 refinery.events_admin_types_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Types"),
                 refinery.events_admin_types_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
